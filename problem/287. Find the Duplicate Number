

int findDuplicate(int* nums, int numsSize){
    int *count = calloc(100000, sizeof(int*));
    
    for(int i = 0; i < numsSize; i++){
        if(count[nums[i]]){
            free(count);
            return nums[i];
        }
        count[nums[i]]++;
    }
    free(count);
    
    
    return;
}

